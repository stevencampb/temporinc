std::ostream& operator<<(std::ostream& os, const Fraction& n) {
    // Implementation of the << operator
    return os;
}

std::istream& operator>>(std::istream& is, Fraction& n) {
    // Implementation of the >> operator
    return is;
}
